# PHP Backend Database Configuration (optional - can also be set in api/config.php)
# These environment variables are used by api/config.php if set
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=ehubph_pms
DB_PORT=3306

# Frontend API URL (optional - defaults to /api for same-origin)
# Set this if your API is on a different domain
VITE_API_URL=/api
